<!DOCTYPE html>
<html>
<head>
    <title>My Tickets - TicketFlow</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/custom.css">
</head>
<body class="bg-gray-50 font-sans">
    {% include 'navbar.html.twig' %}

    <div class="padding-container py-10">
        <div class="flex justify-between items-center">
            <h2 class="text-2xl font-semibold">Your Tickets</h2>
            <button id="add-ticket-btn" class="bg-green-400 text-white px-4 py-2 rounded">Add Ticket</button>
        </div>

        <div class="mt-6 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
            {% for ticket in tickets %}
                <div class="ticket-card border p-4 rounded-lg shadow-md bg-white flex flex-col justify-between cursor-pointer hover:shadow-lg transition-shadow"
                     data-ticket-id="{{ ticket.id }}"
                     data-ticket-title="{{ ticket.title }}"
                     data-ticket-description="{{ ticket.description }}"
                     data-ticket-status="{{ ticket.status }}"
                     data-ticket-date="{{ ticket.date }}">
                    <div>
                        <h3 class="font-bold text-lg mb-2">{{ ticket.title }}</h3>
                        <p class="text-sm text-gray-600 mb-2 line-clamp-2">{{ ticket.description }}</p>
                    </div>
                    <div class="text-sm text-gray-500 mt-2">
                        <span class="px-2 py-1 rounded-full text-xs status-{{ ticket.status }}">{{ ticket.status|replace({"_": " "})|title }}</span>
                        <p class="mt-2">{{ ticket.date|date("F d, Y") }}</p>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

    <!-- Add Ticket Modal -->
    <div id="add-ticket-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden">
        <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md">
            <h2 class="text-2xl font-semibold mb-4">Add New Ticket</h2>
            <form action="/tickets.php" method="POST">
                <input type="hidden" name="action" value="add">
                <input type="text" name="title" placeholder="Ticket title" class="border px-3 py-2 rounded w-full mb-4" required>
                <textarea name="description" placeholder="Ticket description" class="border px-3 py-2 rounded w-full mb-4" rows="4" required></textarea>
                <div class="flex justify-end gap-3">
                    <button type="button" id="cancel-add-btn" class="bg-gray-300 text-black px-4 py-2 rounded">Cancel</button>
                    <button type="submit" class="bg-black text-white px-4 py-2 rounded">Add Ticket</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Ticket Details Modal -->
    <div id="ticket-details-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden">
        <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-lg">
            <h2 id="details-title" class="text-2xl font-bold mb-4"></h2>
            <form id="update-ticket-form" action="/tickets.php" method="POST">
                <input type="hidden" name="action" value="update">
                <input type="hidden" id="details-ticket-id" name="ticket_id">
                <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700">Description</label>
                    <textarea id="details-description" name="description" class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2" rows="4"></textarea>
                </div>
                <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700">Status</label>
                    <select id="details-status" name="status" class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2">
                        <option value="open">Open</option>
                        <option value="in_progress">In Progress</option>
                        <option value="closed">Closed</option>
                    </select>
                </div>
                <div class="mb-4 text-sm text-gray-500">
                    <p><strong>Date Created:</strong> <span id="details-date"></span></p>
                </div>
                <div class="flex justify-between items-center">
                    <button type="button" id="delete-ticket-btn" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Delete</button>
                    <div class="flex gap-3">
                        <button type="button" id="cancel-details-btn" class="bg-gray-200 text-black px-4 py-2 rounded hover:bg-gray-300">Cancel</button>
                        <button type="submit" class="bg-black text-white px-4 py-2 rounded">Save</button>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <!-- Confirmation Modal -->
    <div id="confirm-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center hidden">
        <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-sm">
            <h2 class="text-lg font-semibold mb-4">Are you sure?</h2>
            <p class="text-gray-600 mb-6">Are you sure you want to delete this ticket?</p>
            <form id="delete-ticket-form" action="/tickets.php" method="POST">
                <input type="hidden" name="action" value="delete">
                <input type="hidden" id="delete-ticket-id" name="ticket_id">
                <div class="flex justify-end gap-3">
                    <button type="button" id="cancel-delete-btn" class="bg-gray-300 text-black px-4 py-2 rounded">Cancel</button>
                    <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded">Delete</button>
                </div>
            </form>
        </div>
    </div>

        <script src="/js/tickets.js"></script>

        <script src="/js/toast.js"></script>
    </body>

    </html>
